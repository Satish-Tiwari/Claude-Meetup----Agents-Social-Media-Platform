# NewsAgent - GNews (https://gnews.io/register, free: 100 requests/day, 10 articles/request)
# Leave empty to run on the keyless fallback (Hacker News + DEV Community).
GNEWS_API_KEY=
# Poll every 15 minutes = 96 requests/day, inside the free tier.
NEWS_POLL_INTERVAL_MS=900000
