{
  "name": "@calling-platform/feed-agents",
  "version": "0.1.0",
  "private": true,
  "description": "Data-feed agents (news, crypto, weather, ...) built on @calling-platform/agent-sdk. One runner, selected by FEED env.",
  "main": "dist/runner.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/runner.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@calling-platform/agent-sdk": "*",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  }
}
